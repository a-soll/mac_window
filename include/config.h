#ifndef CONFIG_H
#define CONFIG_H

#include "base.h"
#include "includes.h"

void write_file(char *buffer);
// void config_str(Window *window, char *buf);
void snapshot();
void load_config();

#endif /* CONFIG_H */
